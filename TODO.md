# TODO

- [TODO](#todo)
  - [翻译脚本优化](#翻译脚本优化)
    - [await 返回数据的翻译可能有问题](#await-返回数据的翻译可能有问题)
    - [line 宏插入优化](#line-宏插入优化)
    - [变量作用域判断优化](#变量作用域判断优化)
    - [宏判断内存池单个块是否足够大](#宏判断内存池单个块是否足够大)
    - [不出让判断](#不出让判断)
  - [CTX 功能画饼](#ctx-功能画饼)
    - [内存不足时触发异常](#内存不足时触发异常)
    - [delay until](#delay-until)
    - [数据队列](#数据队列)
    - [解耦 ltx](#解耦-ltx)
    - [时间戳调度](#时间戳调度)

## 翻译脚本优化

### await 返回数据的翻译可能有问题

例如：

```c
float data = _await xxx();
```

有时候能翻译成功，有时候可能会翻译出错遗留 type 导致报错，所以目前下面这种写法会更好一点：

```c
float data;
data = _await xxx();
```

需要改进翻译脚本

### line 宏插入优化

翻译前后代码长度不一，所以可能会单步执行时走过头，所以应该更密集地插入 `#line` 宏


### 变量作用域判断优化

目前已经实现了判断变量作用域来决定是否要保存到局部变量结构体（协程帧）里，但是还有一种变量的判断可以优化

例如如下代码：

```c
float data = _await xxx();
printf("%f\n", data);
// 后续 data 没有再被使用或者没有再跨越出让点
```

会翻译成：

```c
    _co_xxx(...); // 此时并不会读取返回值
    return ; // 出让点
_colable_恢复点:
    // 恢复后读取返回值
    _prv_data->data = con->son->prv_data->_coretval_;
    // 后续业务代码
```

严格来讲，这里的 data 实际上并没有跨越出让点，但是还是会被翻译脚本提升到 prv_data 里，造成额外的协程帧保存开销

也就是应该翻译成：

```c
    _co_xxx(...); // 此时并不会读取返回值
    return ; // 出让点
_colable_恢复点:
    // 恢复后读取返回值
    float data = con->son->prv_data->_coretval_;
```

### 宏判断内存池单个块是否足够大

翻译时额外插入如下内容：

```c
#ifdef 使用固定大小块的内存池分配
    #if (sizeof(struct _coval_函数名) > CO_MAX_PRVDATA_SIZE)
        #error "请增大内存池单个块的大小"
    #endif
#endif
```

编译时确保单个块足够使用，运行时超过大小因为会检查虽然不会出错，但是会导致服务起不起来，因为暂时还没写触发异常或打印，可能会很难找到问题所在

### 不出让判断

有些简单的功能也许可以不必出让，或者说已经有结果的情况可以不用出让。  

比如等待事件组，如果事件组已经满足条件还是要先出让一下自己再回来读数据，增加了切换开销

但是需要大改翻译逻辑，翻译后 `_co_xxx` 不能再使用 void 返回，需要根据立即返回值判断是否需要出让（（这个值就叫 Future 吧（

## CTX 功能画饼

### 内存不足时触发异常

目前的动态分配内存的函数在内存不足时只会返回分配失败，并不会触发异常，运行时内存不足只会出现某个服务跑不起来的情况，并不会让系统停掉，可能会很难排查问题。所以需要考虑如何进行异常触发：

是否可以不需要针对不同平台做额外适配；  
是否可以不用创建额外的任务来管理

### delay until

如果有多个周期任务同时触发并且任务执行时间超过 1 个 tick，那么会导致还没执行的任务的定时稍微拖迟，用户层面可以自己实现 delay until，是否需要 ctx 直接提供 api？

也许挤占是一个优点，可以自动均衡负载，例如：

```
不使用 delay until:
|---------|---------|---------|---------|---------|---------|---------|
0tick     1         2         3         4         5         6         7
|t1|                |t1偶然耗时太久|     |t1|                |t1|
    |t2|                           |t2|           |t2|                |t2|

使用 delay until：
|---------|---------|---------|---------|---------|---------|---------|
0tick     1         2         3         4         5         6         7
|t1|                |t1偶然耗时太久|     |t1|                |t1|
    |t2|                           |t2|     |t2|                |t2|
```

t1 和 t2 都是每两个 tick 执行一次，本来是同时创建的紧接执行的，  
但是经过 t1 的挤压，导致 t2 被临时推迟将近 1 个 tick，后续不会再推迟，而且和 t1 的执行时机错开了，所以负载也许会更均衡；t2 的执行时机会更稳定，因为不需要考虑 t1 可能每次执行耗时不同导致 t2 每次执行间隔会有所增减。  
不过可能会导致 tickless 功耗增加

### 数据队列

并不是很想加，如何兼容不同的变量类型，是否引入额外的动态分配？

协程没有抢断能力，任务间没有资源竞争，所以找一个全局变量进行数据同步就可以了，通过 topic 告知对方数据已经准备好了，直接到全局变量里面读就行了，不可能读一半被其它任务修改。

对于中断抢断任务的情况，队列可能是需要的，目前可以由用户自己创建一个 topic，中断里面 push 队列，然后发布 topic，用户任务在队列有数据时直接读取，队列空时等待 topic 并在成功后 pop 队列元素。

所以似乎可以由用户自己构建相应的数据结构，并不需要管太宽？

### 解耦 ltx

有点难

### 时间戳调度

目前是更适合单片机环境的部署，因为单片机都会提供一个 systick 中断，直接在里面处理和弹出软件定时器即可。

但是对于 windows 之类的平台，没有办法提供一个高精度的定时回调，只能调度器和嘀嗒跑在两个线程，会有线程切换开销，以及因为 windows 的 sleep 精度很低，延时会很不精确。

所以需要把调度器和嘀嗒跑在一个线程内，空闲时不断轮询时间戳判断是否到时间和事件是否触发。

需要 ltx 对此进行支持，ctx 层面做不到
