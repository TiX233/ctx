# TODO

- [TODO](#todo)
  - [翻译脚本优化](#翻译脚本优化)
    - [line 宏插入优化](#line-宏插入优化)
    - [宏判断内存池单个块是否足够大](#宏判断内存池单个块是否足够大)
    - [不出让判断](#不出让判断)
  - [CTX 功能画饼](#ctx-功能画饼)
    - [delay until](#delay-until)
    - [解耦 ltx](#解耦-ltx)
    - [时间戳调度](#时间戳调度)
    - [协程控制块体积优化](#协程控制块体积优化)

## 翻译脚本优化

### line 宏插入优化

翻译前后代码长度不一，所以可能会单步执行时走过头，应该更密集地插入 `#line` 宏。  
不过翻译后状态机并没有变复杂多少，只是替换了用到异步关键字的代码，所以不要这个宏也不会难以调试代码，这个功能优先级并不需要很高。

### 宏判断内存池单个块是否足够大

翻译时额外插入如下内容：

```c
#ifdef 使用固定大小块的内存池分配
    #if (sizeof(struct _coval_函数名) > CO_MAX_PRVDATA_SIZE)
        #error "请增大内存池单个块的大小"
    #endif
#endif
```

编译时确保单个块足够使用，运行时超过大小会进入 `ctx_mem_run_out` 钩子，但是最好还是在编译期确定好内存大小。  
但是有些对象可能用户会静态创建不参与到内存池分配所以不用担心超过单个块大小，此时报错就不太好，哪怕 error 改成 warning 也会比较碍眼，而且脚本不可能遍历所有文件来确保这个对象只被静态创建使用。

### 不出让判断

有些简单的功能也许可以不必出让，或者说已经有结果的情况可以不用出让。  

比如等待事件组，如果事件组已经满足条件还是要先出让一下自己再回来读数据，增加了切换开销

但是需要大改翻译逻辑，翻译后 `_co_xxx` 不能再使用 void 返回，需要根据立即返回值判断是否需要出让（（这个值就叫 Future 吧（

这点切换开销应该无所谓吧，真要扣的话用户直接 peek 一下事件组的值套一层 if 判断是否需要 _await 也能达到不出让的效果。

## CTX 功能画饼

### delay until

如果有多个周期任务同时触发并且任务执行时间超过 1 个 tick，那么会导致还没执行的任务的定时稍微拖迟，用户层面可以自己实现 delay until，是否需要 ctx 直接提供 api？

也许挤占是一个优点，可以自动均衡负载，例如：

```
不使用 delay until:
|---------|---------|---------|---------|---------|---------|---------|
0tick     1         2         3         4         5         6         7
|t1|                |t1偶然耗时太久|     |t1|                |t1|
    |t2|                           |t2|           |t2|                |t2|

使用 delay until：
|---------|---------|---------|---------|---------|---------|---------|
0tick     1         2         3         4         5         6         7
|t1|                |t1偶然耗时太久|     |t1|                |t1|
    |t2|                           |t2|     |t2|                |t2|
```

t1 和 t2 都是每两个 tick 执行一次，本来是同时创建的紧接执行的，  
但是经过 t1 的挤压，导致 t2 被临时推迟将近 1 个 tick，后续不会再推迟，而且和 t1 的执行时机错开了，所以负载也许会更均衡；t2 的执行时机会更稳定，因为不需要考虑 t1 可能每次执行耗时不同导致 t2 每次执行间隔会有所增减。  
不过可能会导致 tickless 功耗增加

### 解耦 ltx

有点难

### 时间戳调度

目前是更适合单片机环境的部署，因为单片机都会提供一个 systick 中断，直接在里面处理和弹出软件定时器即可。

但是对于 windows 之类的平台，没有办法提供一个高精度的定时回调，只能调度器和嘀嗒跑在两个线程，会有线程切换开销，以及因为 windows 的 sleep 精度很低，延时会很不精确。

所以需要把调度器和嘀嗒跑在一个线程内，空闲时不断轮询时间戳判断是否到时间和事件是否触发。

需要 ltx 对此进行支持，ctx 层面做不到

### 协程控制块体积优化

目前协程控制块 CCB 的体积和一般 RTOS 线程的任务控制块 TCB 差不多，都在 100 字节左右的量级，但是会有一个问题，每个协程都会有完整的协程控制块，例如：

在 a 函数中 `_await` b 函数，b 函数中 `_await` c 函数，那么此时就会创建三个 CCB。（部分内置函数不会创建 CCB，比如 delay_ticks，详细查看 [开销计算.md](./docs/开销计算.md)）

目前每个 CCB 对象都会有一个软件定时器和一个事件订阅者的实例，但是只有最内层的协程才会用到这些。  
所以，预想状态下，调用链上的协程只需要记住父子关系即可，不需要有完整的控制块，减小内存消耗。  
那么需要考虑如何实现这一能力，以下是目前的几种想法：

* 仅最外层的协程创建有软件定时器和事件订阅者实例，内层的协程借用即可
* 使用时再动态创建软件定时器或事件订阅者实例
