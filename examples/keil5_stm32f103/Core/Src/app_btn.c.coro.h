#ifndef APP_BTN_CORO_H_
#define APP_BTN_CORO_H_

// Auto-generated private data structures for async coroutines

struct _coval_btn_check {
    // 参数
    // (无)

    // 需要持久化的局部变量
    // (无)

    // 返回值
    int _coretval_;
};

struct coro_stu* _co_btn_check(struct coro_stu *father, struct coro_stu *co);

#endif // APP_BTN_CORO_H_
